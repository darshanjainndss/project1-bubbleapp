import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#020617',
  },
  gameArea: {
    flex: 1,
    position: 'relative',
    width: '100%',
    height: '100%',
  },
});
